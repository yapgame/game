export interface IProps {
  // login: string,
  score: string,
  // email: string,
  // first_name: string,
  // second_name: string,
  // display_name: string,
  // phone: string,
  onHandleSubmit: (data: Record<string, string>) => void,
}
